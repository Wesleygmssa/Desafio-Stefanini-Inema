/* eslint-disable no-return-assign */
import styled from 'styled-components';

export const Container = styled.div`
  position: fixed;
  right: 0;
  top:60px;
  padding: 30px;
  overflow: hidden; // tudo que ficar mostrando além do container ficar invisivel.
  z-index: 99;
`;
